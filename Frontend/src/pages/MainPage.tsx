import Header from "../components/Header.tsx";

const MainPage = () => {
    return (
        <main>
            <Header title="Вакансии"/>
            <p>Вакансии</p>
        </main>
    );
};

export default MainPage;